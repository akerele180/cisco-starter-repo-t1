const Header = () => {
  const styles = {
    wrapper: {
      padding: "3rem",
      color: "#5B6193",
    },
  };
  return <header style={styles.wrapper}>Header</header>;
};

export default Header;
